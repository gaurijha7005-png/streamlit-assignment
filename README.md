# Streamlit Map Assignment

This is a simple Streamlit web app that displays a Folium map with two markers.  
Each marker shows the place name and its latitude/longitude.

## How to run
1. Install requirements:
   pip install streamlit folium streamlit-folium

2. Run the app:
   streamlit run app.py
